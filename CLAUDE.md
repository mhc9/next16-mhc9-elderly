@AGENTS.md

## Coding Standards
- Use 4 spaces for indentation project-wide.
- Authentication: NextAuth.js implemented. Use `useSession` for client components. Protected routes defined in `middleware.ts`.
